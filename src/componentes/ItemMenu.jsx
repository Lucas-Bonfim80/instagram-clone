export default function ItemMenu(props) {
    return (
        <>
            <span className="icon">{props.icone}</span>
            <span className="text">{props.texto}</span>
        </>
    )
}
